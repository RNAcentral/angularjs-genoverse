{
  "name": "angularjs-genoverse",
  "version": "1.0.0",
  "description": "An AngularJS (1.x) directive, wrapping the Genoverse genome browser.",
  "main": "genoverse.module.js",
  "scripts": {
    "test:unit": "karma start --browsers Firefox --single-run",
    "test:e2e": "protractor test/e2e/protractor.conf.js",
    "clean": "rm -rf dist/*",
    "concat": "cat src/*.js > dist/angular-genoverse.all.js",
    "uglify": "uglifyjs --compress --mangle -o dist/angular-genoverse.min.js --source-map dist/angular-genoverse.min.js.map dist/angular-genoverse.all.js",
    "build": "npm run clean && npm run concat && npm run uglify"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BurkovBA/angular-genoverse.git"
  },
  "keywords": [
    "bioinformatics",
    "genome-browser",
    "genoverse",
    "angular",
    "directive",
    "component"
  ],
  "author": "Boris A. Burkov",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/BurkovBA/angular-genoverse/issues"
  },
  "homepage": "https://github.com/BurkovBA/angular-genoverse#readme",
  "devDependencies": {
    "angular": "^1.6.2",
    "jquery": "^3.1.1",
    "uglify-js": "^2.7.5",
    "bootstrap": "^3.3.7"
  },
  "dependencies": {
  }
}
